{
  "rules": {
    "users": {
    	".read": true,
    	".write": "root.child('users').child(auth.provider).child(auth.uid).child('roles/admin').val() == true"
    },
  	"schools": {
    	".read": true,
			".write": "root.child('users').child(auth.provider).child(auth.uid).child('roles/admin').val() == true"
    }
  }
}
